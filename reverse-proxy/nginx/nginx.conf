# TODO: configure Nginx base settings for reverse proxy
# Example placeholders only; do not place secrets here.

user nginx;
worker_processes auto;

events {
  worker_connections 1024;
}

http {
  # TODO: include basic settings, logging, and mime types
  include       mime.types;
  default_type  application/octet-stream;

  # TODO: set access_log and error_log paths

  include /etc/nginx/conf.d/*.conf;
  include /etc/nginx/sites-enabled/*.conf;
}
