# TODO: configure Matrix reverse proxy for Synapse and Element
# Example placeholders only; do not place secrets here.

server {
  listen 80;
  server_name matrix.example.com;

  # TODO: add TLS redirect, headers, and upstream proxy settings
}
